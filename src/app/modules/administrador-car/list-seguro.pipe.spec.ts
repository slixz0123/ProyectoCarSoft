import { ListSeguroPipe } from './list-seguro.pipe';

describe('ListSeguroPipe', () => {
  it('create an instance', () => {
    const pipe = new ListSeguroPipe();
    expect(pipe).toBeTruthy();
  });
});
